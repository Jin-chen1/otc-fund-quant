# 1. 激活环境
conda activate fund
022464
# 2. 运行回测脚本
python "d:\基金分析代码\otc_fund_quant\tests\run_simulation.py"

# 3. 运行绘图脚本
python "d:\基金分析代码\otc_fund_quant\tools\plot_results.py"

# 4. 运行完上述命令后，请打开文件夹：
D:\基金分析代码\otc_fund_quant\results\

修改目标基金: 编辑 d:\基金分析代码\otc_fund_quant\tests\run_simulation.py，修改 fund_code = '005658' 为其他代码（如 000001 上证指数）。
修改策略参数: 编辑 d:\基金分析代码\otc_fund_quant\strategies\trend_strategy.py，调整 ma20, ma60 或 percentile 阈值。

---

# 5. 启动 Web 交互界面

## 启动命令
conda activate fund; python -m web.app

cd d:\基金分析代码\otc_fund_quant
python -c "import sys; sys.path.insert(0, r'd:\基金分析代码'); from otc_fund_quant.web.app import app; app.run(debug=False, host='127.0.0.1', port=5000)"

## 访问地址
浏览器打开: http://127.0.0.1:5000

## 功能说明
- **基金代码输入**: 在顶部输入任意场外基金代码（如 007343），点击"分析"或按回车
- **策略切换**: 策略由后端按基金画像动态返回。A股主动权益默认开放 V6 + Regime Adaptive，指数画像可额外开放 Index Momentum
- **基金走势图**: 交互式图表，含 NAV + MA20/MA60 均线、RSI(14)、MACD 柱状图，支持缩放和悬停查看
- **买卖信号点**: 图表上标注历史买入/卖出信号，按策略分别展示
- **今日买卖判断**: 基于选定策略自动给出 BUY/SELL/HOLD 建议及理由
- **区间收益对比**: 展示近5年/3年/1年/半年/1月的策略回测收益 vs 基金涨跌幅（使用真实回测引擎计算）
- **历史记录**: 底部展示最近200条建议记录及对错标注，按策略分别存储

## 策略说明
- **V6**: 估值趋势混合策略，三重过滤买入（金叉/RSI/MACD），分批建仓，分级止损(-18%/-25%)，冷静期保护
- **Regime Adaptive**: 在V6基础上叠加ADX市场状态识别（BULL/BEAR/RANGE），动态调整买卖阈值，牛市DCA加速
- **Index Momentum**: 适用于指数基金/ETF联接，用布林带 + 动量评分 + ATR 自适应仓位管理

## 配置说明
- 新配置优先读取 `config/strategy_profiles.json`
- 旧配置 `config/strategy_params.json` 仍作为兼容回退源
- 旧配置迁移到新画像配置:
  - `conda activate fund`
  - `python tools/migrate_strategy_config.py --force`

---

# 6. 项目结构

```
otc_fund_quant/
├── __init__.py
├── requirements.txt         # 依赖管理
├── 命令.md                   # 项目说明文档
├── config/                  # 策略画像与参数配置
│   ├── __init__.py
│   ├── loader.py            # 配置加载器（基金类型 -> 画像 -> 策略 -> 参数）
│   ├── strategy_profiles.json # 新画像分层配置文件（优先）
│   └── strategy_params.json # 旧策略参数配置文件（兼容回退）
├── analysis/                # 通用分析模块（指标、信号、回测、图表）
│   ├── __init__.py
│   ├── indicators.py        # 技术指标计算（唯一入口，参数化）
│   ├── signals.py           # 买卖建议生成、信号点标记、历史回顾
│   ├── backtest.py          # 区间回测（调用真实策略类，T+1结算）
│   ├── chart.py             # 图表数据格式化
│   └── strategy_registry.py # 策略注册表（元数据 + 生成函数）
├── core/                    # 回测基础设施
│   ├── __init__.py
│   ├── account.py           # 账户管理（现金、持仓）
│   ├── engine.py            # 回测引擎（T+1结算循环）
│   ├── order.py             # 订单模型
│   └── position.py          # 持仓管理（FIFO、赎回费）
├── data/
│   ├── __init__.py
│   ├── loader.py            # 数据加载（akshare + SQLite缓存）
│   └── db.sqlite            # 基金NAV数据库
├── strategies/
│   ├── __init__.py
│   ├── trend_strategy.py    # V6 估值趋势混合策略
│   ├── regime_adaptive_strategy.py  # 市场状态自适应策略
│   └── index_momentum_strategy.py   # 指数动量策略
├── web/
│   ├── __init__.py
│   ├── app.py               # Flask 路由（导入 analysis.* 模块）
│   ├── recommendations.db   # 建议记录+回测缓存数据库（自动生成）
│   └── templates/
│       └── index.html       # 前端页面
├── tests/
│   ├── run_simulation.py    # V6策略回测脚本
│   ├── run_regime_strategy.py  # 双策略对比回测脚本
│   └── test_simulation_mock.py # 模拟测试
├── tools/
│   ├── migrate_strategy_config.py # 旧策略配置迁移脚本
│   └── plot_results.py      # 回测结果绘图
└── results/                 # 回测输出
    ├── equity.csv            # 权益曲线数据
    ├── equity.png            # 权益曲线图
    └── trades.csv            # 交易记录
```
